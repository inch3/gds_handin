## git history remark

We didn't actually use a git repo while working on this project. The repo was only set up at the end for the hand-in, which is why the git log doesn't reflect the actual development process.

## setup
Install dependencies via `pixi install` from the project's `pixi.toml`. The committed `pixi.lock` reproduces the exact dependency set.

Activate the environment with:

```
pixi shell
```
